# Section 4 — C4 Architecture

Principles: **Cloud Native** (containers on AKS, selective serverless), **Zero Trust**
(every hop authenticated), **Hexagonal** (L4 shows ports & adapters), **12-Factor**
(stateless services, env config).

## 4.1 L1 — System Context

```mermaid
flowchart TB
  visitor(("Visitor"))
  host(("Employee Host"))
  soc(("Security Operator"))
  approver(("Approver / Area Owner"))
  auditor(("Compliance Auditor"))

  AMS["AMS - Enterprise Access Management System<br/>(access decisions, visitor ops, badge lifecycle,<br/>occupancy, evacuation, immutable audit)"]

  entra["Microsoft Entra ID<br/>(workforce identity, OIDC, Conditional Access)"]
  hr["HR / Contract System"]
  email["Email / Teams / Push providers"]
  edge["Site Edge: gateways, readers,<br/>kiosks, badge printers"]
  siem["Microsoft Sentinel (SIEM)"]

  visitor -->|"pre-registration link, kiosk"| AMS
  host -->|"web portal"| AMS
  soc -->|"SecOps dashboard"| AMS
  approver -->|"web / deep links"| AMS
  auditor -->|"evidence queries, report packs"| AMS

  AMS <-->|"SCIM/Graph sync, OIDC"| entra
  hr -->|"contract windows"| AMS
  AMS -->|"notifications"| email
  AMS <-->|"config snapshots down, decisions up (mTLS)"| edge
  AMS -->|"security events"| siem
```

## 4.2 L2 — Container Diagram

```mermaid
flowchart TB
  subgraph Clients
    spa["Web App<br/>React 19 + MUI v7 (SPA, Vite)"]
    kiosk["Kiosk App<br/>(React, locked-down)"]
    edgeGw["Edge Gateway<br/>(.NET 10 self-contained, per site)"]
  end

  afd["Azure Front Door + WAF"]
  apim["Azure API Management<br/>(OAuth2 validation, rate limits, versioning)"]

  subgraph AKS["AKS (West Europe primary / North Europe secondary)"]
    idsvc["Identity Service"]
    vissvc["Visitor Service"]
    bdgsvc["Badge Service (event-sourced)"]
    accsvc["Access Control Service"]
    aprsvc["Approval Service"]
    ntfsvc["Notification Service"]
    audsvc["Audit Service"]
    sitesvc["Site & Topology Service"]
    occsvc["Occupancy & Evacuation Service"]
    rptsvc["Compliance Reporting Service"]
  end

  eh["Azure Event Hubs<br/>(Kafka protocol, partitioned by aggregate id)"]
  redis["Azure Cache for Redis<br/>(read cache, idempotency, pub/sub)"]
  pg[("PostgreSQL 18 Flexible Server<br/>(DB-per-service schemas)")]
  worm[("Blob Storage - immutable WORM<br/>(audit archive)")]
  kv["Key Vault Premium (HSM)"]
  entra2["Entra ID"]
  mon["Azure Monitor / OTel Collector<br/>Prometheus - Grafana - Loki - Tempo"]

  spa --> afd
  kiosk --> afd
  edgeGw -->|"mTLS + device identity"| afd
  afd --> apim
  apim --> AKS
  AKS <--> eh
  AKS <--> redis
  AKS --> pg
  audsvc --> worm
  AKS -.->|"secrets, signing keys"| kv
  apim -.->|"JWT validation"| entra2
  AKS -.->|"OTLP"| mon
```

DB-per-service is strict (Section 6 matrix): one Flexible Server per region hosts
isolated databases/roles per service; no cross-service SQL, ever (enforced by network
policy + credentials).

## 4.3 L3 — Component: Access Control Service

```mermaid
flowchart TB
  subgraph AccessControlService["Access Control Service (hexagonal)"]
    api["Inbound adapter: Minimal API<br/>/v1/badge-validations, /v1/access-requests, grants"]
    grpcIn["Inbound adapter: gRPC ValidationService<br/>(edge gateways, high-throughput)"]
    consumer["Inbound adapter: Event Hubs consumer<br/>(cardholder + badge + approval events)"]
    app["Application layer: MediatR pipeline<br/>(validation behavior, logging, idempotency)"]
    pipeline["Domain: DecisionPipeline<br/>credential -> cardholder -> grant -> schedule<br/>-> ABAC -> anti-passback -> capacity"]
    policy["Domain: AccessPolicy + AccessGrant aggregates"]
    occ["Domain: ZoneOccupancy + AntiPassbackTracker"]
    repoP["Outbound port: IAccessGrantRepository / IAccessPolicyRepository"]
    decP["Outbound port: IDecisionLogWriter (append-only)"]
    snapP["Outbound port: ICredentialSnapshotReader"]
    obxP["Outbound port: IIntegrationEventPublisher (outbox)"]
    pgA["Adapter: Npgsql -> access_control DB"]
    redisA["Adapter: Redis (snapshot cache, anti-passback state)"]
    ehA["Adapter: Outbox dispatcher -> Event Hubs"]
  end

  api --> app
  grpcIn --> app
  consumer --> app
  app --> pipeline
  pipeline --> policy
  pipeline --> occ
  pipeline --> snapP
  app --> repoP
  app --> decP
  app --> obxP
  repoP --> pgA
  decP --> pgA
  snapP --> redisA
  obxP --> ehA
```

## 4.4 L3 — Component: Badge Service

```mermaid
flowchart TB
  subgraph BadgeService["Badge Service (hexagonal, event-sourced)"]
    api2["Inbound adapter: Minimal API /v1/badges"]
    cons2["Inbound adapter: Event Hubs consumer<br/>(VisitorCheckedIn, CardholderSuspended, ...)"]
    app2["Application: MediatR commands/queries<br/>+ FluentValidation + Idempotency behavior"]
    agg["Domain: Badge aggregate (Apply/Raise,<br/>state-machine invariants)"]
    signer["Domain port: IQrSigner"]
    repo2["Outbound port: IBadgeRepository<br/>(LoadAsync=replay, AppendAsync=optimistic)"]
    proj["Projection worker: stream -> badge_current_state<br/>+ credential_validation_snapshot"]
    obx2["Outbound port: IIntegrationEventPublisher (outbox)"]
    es[("Adapter: PostgreSQL event store<br/>(badge_events append-only)")]
    kvA["Adapter: Key Vault signer (key rotation)"]
    eh2["Adapter: Outbox -> Event Hubs"]
    rm[("Adapter: read-model tables + Redis snapshot")]
  end

  api2 --> app2
  cons2 --> app2
  app2 --> agg
  agg --> signer
  signer --> kvA
  app2 --> repo2
  repo2 --> es
  es --> proj
  proj --> rm
  app2 --> obx2
  obx2 --> eh2
```

## 4.5 L4 — Code structure (Badge Service, hexagonal layout)

Implemented for real under `src/` in this repository:

```
src/
├── Ams.sln
├── Directory.Build.props                  # net10.0, C# 14, nullable, analyzers
├── BuildingBlocks/
│   └── Ams.BuildingBlocks/                # tiny shared kernel (ADR-013)
│       ├── Domain/
│       │   ├── AggregateRoot.cs           # Raise/Apply base, version tracking
│       │   ├── IDomainEvent.cs
│       │   └── StronglyTypedId.cs
│       ├── Messaging/
│       │   ├── IIntegrationEventPublisher.cs
│       │   └── OutboxMessage.cs
│       └── Web/
│           └── IdempotencyKeyMiddleware.cs
└── Services/Badge/
    ├── Ams.Badge.Domain/                  # pure domain: no framework refs
    │   ├── Badge.cs                       # aggregate root (event-sourced)
    │   ├── BadgeEvents.cs                 # strongly-typed event records
    │   ├── BadgeState.cs / BadgeType.cs / ValidityWindow.cs
    │   ├── BadgeErrors.cs
    │   └── IBadgeRepository.cs            # outbound port
    ├── Ams.Badge.Application/             # use cases (MediatR)
    │   ├── IssueBadge/
    │   │   ├── IssueBadgeCommand.cs
    │   │   ├── IssueBadgeHandler.cs
    │   │   └── IssueBadgeValidator.cs
    │   ├── RevokeBadge/RevokeBadgeCommand.cs (+ handler/validator)
    │   └── Behaviors/ValidationBehavior.cs
    ├── Ams.Badge.Infrastructure/          # adapters
    │   ├── Persistence/PostgresBadgeRepository.cs
    │   ├── Persistence/PostgresOutboxStore.cs
    │   ├── Messaging/EventHubsProducer.cs
    │   └── Messaging/OutboxDispatcher.cs  # BackgroundService
    └── Ams.Badge.Api/                     # inbound HTTP adapter
        ├── Program.cs                     # Minimal API, RFC 7807, OTel, health
        └── Endpoints/BadgeEndpoints.cs
```

Dependency rule (enforced by project references + architecture tests): `Api → Application
→ Domain`; `Infrastructure → Application + Domain`; **Domain references nothing** but
`Ams.BuildingBlocks.Domain`. That is Hexagonal/Ports-and-Adapters made compile-time
checkable (SOLID-D, Clean Architecture).

<!-- SECTION 4 COMPLETE -->
