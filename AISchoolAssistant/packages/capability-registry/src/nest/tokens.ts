/** DI token for the {@link AiProviderRegistry}. */
export const AI_PROVIDER_REGISTRY = 'AI_PROVIDER_REGISTRY';

/** DI token for the {@link ModelRouter}. */
export const MODEL_ROUTER = 'MODEL_ROUTER';

/** DI token for the {@link CapabilityRegistry}. */
export const CAPABILITY_REGISTRY = 'CAPABILITY_REGISTRY';

/** DI token for the {@link AiObservabilitySink}. */
export const AI_OBSERVABILITY = 'AI_OBSERVABILITY';

/** DI token for the list of capabilities to register + publish at boot. */
export const AI_CAPABILITIES = 'AI_CAPABILITIES';
