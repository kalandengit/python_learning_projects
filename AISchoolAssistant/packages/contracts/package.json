{
  "name": "@asa/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Shared platform contracts: pagination, problem+json, health.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.0.5"
  }
}
