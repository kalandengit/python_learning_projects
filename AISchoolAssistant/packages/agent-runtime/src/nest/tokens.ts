/** DI token for the agent-runtime {@link AiProviderRegistry}. */
export const AGENT_PROVIDER_REGISTRY = 'AGENT_PROVIDER_REGISTRY';

/** DI token for the agent-runtime {@link ModelRouter}. */
export const AGENT_MODEL_ROUTER = 'AGENT_MODEL_ROUTER';

/** DI token for the {@link ToolRegistry}. */
export const TOOL_REGISTRY = 'TOOL_REGISTRY';

/** DI token for the {@link AgentRegistry}. */
export const AGENT_REGISTRY = 'AGENT_REGISTRY';

/** DI token for the {@link AgentObservabilitySink}. */
export const AGENT_OBSERVABILITY = 'AGENT_OBSERVABILITY';
