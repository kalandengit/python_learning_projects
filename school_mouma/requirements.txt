# PROJET_SCHOOL_MOUMA_BKO_2026 - dependencies
# Core framework
Django==5.0.6

# Configuration & environment
python-dotenv==1.0.1
dj-database-url==2.2.0

# Database (production)
psycopg[binary]==3.1.19

# PDF generation (bulletins, reçus, fiches)
weasyprint==62.3

# Static files serving in production
whitenoise==6.7.0

# WSGI server (production)
gunicorn==22.0.0

# --- Development / test only ---
pytest==8.2.2
pytest-django==4.8.0
