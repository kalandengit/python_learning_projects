# Frontend configuration. Copy to .env.local for local dev (never commit secrets).

# Absolute API base overrides the dev proxy. Leave unset to use Vite's /api proxy
# to the FastAPI backend on :8000 (see vite.config.ts).
# VITE_API_BASE_URL=http://localhost:8000/api/v1
