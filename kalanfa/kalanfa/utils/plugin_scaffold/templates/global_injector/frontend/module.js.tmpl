// This module is injected on every page by the FrontEndBaseSyncHook in
// kalanfa_plugin.py. Add global side effects here — analytics, monitoring,
// global event listeners, and so on.
//
// To read data exposed from the Python side, import it and add a
// ``plugin_data`` property to the WebpackBundleHook in kalanfa_plugin.py:
//
//     import plugin_data from 'kalanfa-plugin-data';
