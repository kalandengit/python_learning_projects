# Runtime dependencies for the PIDS MVP backend.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
pydantic[email]>=2.7
SQLAlchemy>=2.0
python-multipart>=0.0.9

# Production extras (uncomment / pin as needed):
# psycopg[binary]>=3.1        # PostgreSQL driver
# redis>=5.0                  # distributed dedup / cache
# argon2-cffi>=23.1           # stronger password hashing (replace stdlib PBKDF2)
# PyJWT[crypto]>=2.8          # vetted JWT with asymmetric keys
# confluent-kafka>=2.4        # Kafka event bus
# boto3>=1.34                 # S3/MinIO media storage
# opentelemetry-sdk>=1.25     # tracing/metrics

# Test/dev
pytest>=8.0
httpx>=0.27
