# No AndroidX: the app uses only the Android framework (no support libraries),
# which keeps the build dependency-free and fast.
android.useAndroidX=false
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
